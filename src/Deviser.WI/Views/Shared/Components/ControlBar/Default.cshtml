@using System.Security.Claims
@using Deviser.Core.Library
@using AppContext = Deviser.Core.Library.AppContext;
@{
    Layout = null;
    AppContext deviserContext = (AppContext)ViewBag.AppContext;  //HttpContext.Current.Session["AppContext"];
}

@if (User.Identity.IsAuthenticated)
{
    <!-- BEGIN HEADER -->
    <div class="sd-controlbar">
        <!-- BEGIN LOGO -->
        <div class="sd-logo">
            <a class="sd-navbar-brand" href="/en-us/admin">
                <img src="/images/DeviserLogoWhite.png" alt="DEVISER">
            </a>
        </div>
        <!-- END LOGO -->
        <!-- END RESPONSIVE MENU TOGGLER -->
        <!-- BEGIN TOP NAVIGATION MENU -->
        <div class="top-menu">
            <form sd-module="Security" sd-controller="Account" sd-action="LogOff" method="post" id="logoutForm">
                <ul class="sd-navbar-nav">
                    <li class="dropdown dropdown-quick-sidebar-toggler">
                        <a href="/@deviserContext.CurrentLink" class="dropdown-toggle">
                            <i class="fa fa-desktop"></i>
                            <span>View</span>
                        </a>
                    </li>
                    <li class="dropdown dropdown-quick-sidebar-toggler">
                        <a href="@Url.Action("Edit", "Page")" class="dropdown-toggle">
                            <i class="fa fa-pencil-square-o"></i>
                            <span>Edit</span>
                        </a>
                    </li>

                    <li class="dropdown dropdown-quick-sidebar-toggler">
                        <a href="@Url.Action("Layout", "Page")" class="dropdown-toggle">
                            <i class="fa fa-columns"></i>
                            <span>Layout</span>
                        </a>
                    </li>
                    <li class="dropdown dropdown-user">
                        <a href="javascript:;" class="dropdown-toggle" data-toggle="dropdown" data-hover="dropdown" data-close-others="true">
                            @*<img alt="" class="img-circle" src="../../assets/admin/layout/img/avatar3_small.jpg" />*@
                            <span class="username username-hide-on-mobile">
                                @User.Identity.Name
                            </span>
                            <i class="fa fa-angle-down"></i>
                        </a>
                        <ul class="sd-dropdown-menu dropdown-menu dropdown-menu-default">
                            <li>
                                <a asp-controller="Manage" asp-action="Index" title="Manage">
                                    <i class="icon-user"></i> My Profile
                                </a>
                            </li>
                            <li>
                                <a href="javascript:document.getElementById('logoutForm').submit()">
                                    <i class="icon-logout"></i> Log Out
                                </a>
                            </li>
                        </ul>
                    </li>
                </ul>
            </form>

        </div>
        <!-- END TOP NAVIGATION MENU -->
    </div>
        <!-- END HEADER INNER -->

}
else
{
    @*<ul class="nav navbar-nav navbar-right">
            <li><a asp-controller="Account" asp-action="Register">Register</a></li>
            <li><a asp-controller="Account" asp-action="Login">Log in</a></li>
        </ul>*@
}
