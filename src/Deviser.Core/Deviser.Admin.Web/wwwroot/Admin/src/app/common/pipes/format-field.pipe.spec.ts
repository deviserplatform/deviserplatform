import { FormatFieldPipe } from './format-field.pipe';

describe('FormatFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
