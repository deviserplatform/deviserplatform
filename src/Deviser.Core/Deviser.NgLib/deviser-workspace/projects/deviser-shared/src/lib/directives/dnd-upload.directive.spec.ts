import { DndUploadDirective } from './dnd-upload.directive';

describe('DndUploadDirective', () => {
  it('should create an instance', () => {
    const directive = new DndUploadDirective();
    expect(directive).toBeTruthy();
  });
});
